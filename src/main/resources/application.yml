spring:
  application:
    name: First Spring Application
  jpa:
    database: h2
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password: password

logging:
  level:
    org:
      springframework: INFO
      hibernate: DEBUG
      mik: DEBUG




